const MAX_LENGTH = 20;
const MAX_HEIGHT = 20;


export const createGrid = () => {
    const grid = new Array(MAX_HEIGHT);
    for (let i = 0; i < MAX_HEIGHT; i++) {
        grid[i] = new Array(MAX_LENGTH);
    }
    return grid;
};

const randomiseGrid = (grid) => {
    return newGrid;
};

const calculateGridNextGen = (grid) => {
    return newGrid;
};